# Web框架
fastapi==0.115.6
starlette==0.41.3  # 与 fastapi 0.115.x 兼容；不锁定会漂移到 1.x 导致启动崩溃
uvicorn[standard]==0.34.0
python-multipart==0.0.19

# 文档解析
PyPDF2==3.0.1
pdfplumber==0.11.4
python-docx==1.2.0

# 大模型API
openai==1.59.3

# 图数据库
neo4j==5.27.0

# RAG框架（可选）
langchain==0.3.14
langchain-community==0.3.14
chromadb==0.5.23

# 文本处理
tiktoken==0.8.0
jieba==0.42.1

# 向量检索（L2 阶段 E：VectorIndex 的 numpy 矩阵后端）
# 未安装时 `services/vector_index.py` 会自动回落纯 Python 余弦（功能可用，但慢 100×+），
# 所以这里是**显式声明**而非隐性依赖——此前 numpy 只被离线脚本 eval_*.py 用到，requirements 未声明。
numpy==1.26.4

# 工具
pydantic==2.10.4
python-dotenv==1.0.1
aiofiles==24.1.0
PyJWT==2.12.1

# 评测报告出图（仅 create_graphy.py 使用，跑后端服务不需要它）
# 中文标签依赖系统字体：Windows 自带 Microsoft YaHei/SimHei 即可；
# Linux 需另装 fonts-noto-cjk，否则图中的中文会渲染成方块
matplotlib==3.10.7
